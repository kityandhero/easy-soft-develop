declare const _exports: {
  ignoreFile: {
    folderPath: string;
    fileName: string;
    coverFile: boolean;
    fileContent: string;
  };
  contentFile: {
    folderPath: string;
    fileName: string;
    coverFile: boolean;
    fileContent: string;
  };
  configFile: {
    folderPath: string;
    fileName: string;
    coverFile: boolean;
    fileContent: string;
  };
};
export = _exports;
